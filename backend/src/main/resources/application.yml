server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:55432/examdb
    username: postgres
    password: postgres
    hikari:
      # Force connection session timezone to UTC to avoid invalid aliases like "Asia/Saigon"
      data-source-properties:
        options: -c TimeZone=UTC
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        jdbc:
          'time_zone': UTC
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration

# CORS (cho FE local)
# cors.allowed-origins is read via @Value with default; keep here commented for reference
# cors:
#   allowed-origins: "http://localhost:3000,http://localhost:5173"

# Media storage
# media.upload-dir is read via @Value with default; keep here commented for reference
# media:
#   upload-dir: ${MEDIA_UPLOAD_DIR:/app/uploads}

